# Weather agent container (custom variation). One Microsoft Agent Framework
# agent is composed with framework-neutral Responses and A2A helpers in an
# app-owned Starlette server.

agent-framework-core==1.16.0
agent-framework-foundry==1.11.0
agent-framework-openai==1.14.1
agent-framework-a2a==1.0.0b260821
agent-framework-hosting-responses==1.0.0a260730

# ASGI routes used to compose the native protocol adapters.
starlette==1.6.0
uvicorn==0.52.4

# MCP client used by Agent Framework to reach the direct weather MCP URL. Not
# pulled in transitively by the pins above.
mcp==2.1.1

# Native A2A serving. The http-server extra provides the Starlette route layer.
a2a-sdk[http-server]==1.1.2

# Azure SDKs + telemetry
azure-identity==1.26.0b2
azure-monitor-opentelemetry==1.8.9
python-dotenv==1.2.3
