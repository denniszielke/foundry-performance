# Weather agent container (custom variation only — no azure-ai-agentserver
# packages here; the responses/invocations/invocations_ws protocols are served
# by hand-written Starlette routes in multiprotocol.py instead).
# Pins mirror the repository's existing dependency set.

agent-framework-core==1.12.1
agent-framework-foundry==1.10.3

# ASGI app + server for the hand-rolled responses/invocations/invocations_ws
# routes (previously pulled in transitively via azure-ai-agentserver-core).
starlette==1.3.1
uvicorn==0.49.0

# MCP client used by Agent Framework to reach the weather tool (direct MCP URL
# or Foundry toolbox endpoint). Not pulled in transitively by the pins above.
mcp==1.28.1

# Native A2A serving. a2a-sdk 1.0 was a breaking rewrite (protobuf types,
# route-factory functions instead of the removed A2AStarletteApplication
# wrapper) — a2a_app.py targets the 1.x API. The http-server extra provides
# starlette/sse-starlette; without it the A2A endpoint is silently disabled.
a2a-sdk[http-server]==1.1.2

# Azure SDKs + telemetry
azure-identity==1.25.3
azure-monitor-opentelemetry==1.8.9
python-dotenv==1.2.2
