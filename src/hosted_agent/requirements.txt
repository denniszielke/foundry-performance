# Weather agent container (hosted + custom variations share this set).
# Microsoft Agent Framework + azure-ai-agentserver multi-protocol hosts.
# Pins mirror the repository's existing dependency set.

agent-framework-core==1.12.1
agent-framework-foundry==1.10.3

# Protocol hosts (responses / invocations / invocations_ws)
azure-ai-agentserver-core==2.0.0b8
azure-ai-agentserver-responses==1.0.0b9
azure-ai-agentserver-invocations==1.0.0b6

# Native A2A serving. Exact pin may need adjusting to the installed SDK — the
# AgentCard field names have drifted across a2a-sdk releases.
a2a-sdk>=0.3.0

# Azure SDKs + telemetry
azure-identity==1.25.3
azure-monitor-opentelemetry==1.8.9
python-dotenv==1.2.2
