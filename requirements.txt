# All service dependencies for local development + the deploy scripts.
# Per-service requirements (src/*/requirements.txt) mirror the relevant subset.
# Only packages imported by the code are listed.

# --- Weather MCP server (src/weather_mcp_server) ---------------------------
fastmcp==3.4.5
mcp==1.28.1
starlette==1.3.1
uvicorn==0.51.0

# --- Weather agents (src/hosted_agent_responses, src/hosted_agent_invocations, src/custom_agent) ---
# Microsoft Agent Framework + azure-ai-agentserver protocol hosts.
agent-framework-core==1.12.1
agent-framework-foundry==1.10.3
# Built-in ResponsesHostServer (hosted_agent_responses); pulls in
# azure-ai-agentserver-core/-responses transitively.
agent-framework-foundry-hosting==1.0.0b260722
azure-ai-agentserver-invocations==1.0.0b7
# Native invocations AG-UI host with Toolbox MCP.
ag-ui-protocol==0.1.16
openai==2.32.0
pydantic-ai-slim[ag-ui,mcp,openai]==1.75.0
# Native A2A serving (AgentCard field names have drifted across releases).
a2a-sdk>=0.3.0

# --- Benchmark clients (src/clients) ---------------------------------------
httpx==0.28.1
websockets==16.1.1

# --- Deployment scripts (scripts/, Foundry management plane) ---------------
# azure-ai-projects is capped at <2.4.0 by agent-framework-foundry 1.10.3.
azure-ai-projects==2.3.0

# --- Shared Azure SDKs + telemetry -----------------------------------------
azure-identity==1.25.3
azure-monitor-opentelemetry==1.8.9
python-dotenv==1.2.2
